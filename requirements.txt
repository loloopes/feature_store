# correct_phoenix — Feast + ML stack (Python 3.12)
# Install: pip install -r requirements.txt
#
# DVC is in requirements-dvc.txt (separate env recommended — see below).

# S3 stack pinned for Feast 0.63 (fsspec<=2024.9.0)
fsspec==2024.9.0
s3fs==2024.9.0
aiobotocore==2.23.1
boto3==1.38.27
botocore==1.38.46

# Feast feature store
feast[aws,redis]==0.63.0

# ML / data
pandas==2.3.3
numpy==2.3.5
scikit-learn==1.8.0
pyarrow==24.0.0
joblib==1.5.3
scipy==1.17.1

# Online store + utils
redis==7.4.0
hiredis==3.3.1
tqdm==4.67.3
python-dotenv==1.2.2

# Notebook (optional — remove if not needed)
ipykernel==6.29.5
